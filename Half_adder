def Xor_Gate(a,b):
    if (a!=b):
        return 1
    return 0

def And_Gate(a,b):
    if (a==1 and b==1):
        return 1
    return 0

a=int(input('a:'))
b=int(input('b:'))
sum=Xor_Gate(a,b)
carry=And_Gate(a,b)
print(sum)
print(carry)
